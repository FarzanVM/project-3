import { FormdirectiveDirective } from './formdirective.directive';

describe('FormdirectiveDirective', () => {
  it('should create an instance', () => {
    // const directive = new FormdirectiveDirective();
    // expect(directive).toBeTruthy();
  });
});
